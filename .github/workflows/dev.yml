name: Dev
on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: fake build
        env:
          GITHUB_EVENT: ${{ toJson(github.event) }}
        run: |
          echo Git head: "$(git rev-parse --short HEAD)"
          echo GitHub Event:
          echo "$GITHUB_EVENT" | jq -aM
